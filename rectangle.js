const rectangle = {
    "length": 8,
    "width": 10,
  };

  function calculateArea(length, width) {
    console.log("Plocha obdĺžnika: " + length * width + "m²");
};

calculateArea(rectangle.length, rectangle.width);